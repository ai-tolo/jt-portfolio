# build output
dist/
# Vercel-adapter build output (created by `astro build` when output: 'server').
# Vercel does its own build server-side; committing these artifacts hijacks
# the deploy and bakes local .env values into the function.
.vercel/
# generated types
.astro/

# dependencies
node_modules/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
.env.development.local
.env.production.local
.env.production
.env.*.local

# macOS-specific files
.DS_Store

# editor / tool folders
.idea/
.claude/
.vercel
