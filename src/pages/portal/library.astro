---
// Library page removed 2026-05-17 evening. Buckets is now the library:
// search, sort (incl. most-used-in-projects), source + content-type chips,
// Random, plus the per-card triage actions that Library never had. Trash
// is reachable as the TRASH sub-tab.
return Astro.redirect("/portal/buckets/all", 307);
---
